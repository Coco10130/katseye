import 'package:flutter/material.dart';

class SoldOutProducts extends StatelessWidget {
  const SoldOutProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Sold Out Prodcuts");
  }
}
