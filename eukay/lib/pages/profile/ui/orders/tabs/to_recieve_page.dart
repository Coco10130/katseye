import 'package:flutter/material.dart';

class ToRecieve extends StatelessWidget {
  const ToRecieve({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
